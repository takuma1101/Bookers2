<% if flash[:notice] %>
 <p id=notice><%= flash[:notice] %></p>
<% else %>
 <p id=notice></p>
<% end %>
<%= render 'books/error', model: @book %>
 <div class="container">
    <div class="row">
      <div class="col-md-3">
       <h2>User info</h2>
       <%= image_tag @books.user.get_profile_image(100,100)  %>
 <table class="table">
  <tbody>
    <tr></tr>
  <tr>
	  <th>name</th>
	  <th><%= @users.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= @users.introduction %></th>
  </tr>
</tbody>
</table>

<div class="row">
   <%= link_to edit_user_path(@users),class: "btn btn-outline-secondary btn-block edit_user_#{@users.id}" do%>
    <i class="fas fa-user-cog"></i>
<% end %>
</div>
        <h2 class="mt-3">New book</h2>
        <%= render 'books/book', book: @book %>
      </div>
      <div class="col-md-8 offset-md-1">
        <h1>Book detail</h1>
        <table class="table">
         <tbody>
          <tr>
           <td> <%= link_to user_path(@users) do%>
           <%= image_tag @users.get_profile_image(100,100) %></br>
           <%= @users.name %></td><% end %>
            <td><%= link_to  @books.title ,book_path(@books)%></td>
            <td><%= @books.body %></td>
            <% if @books.user_id == current_user.id %>
              <td><%= link_to "Edit", edit_book_path(@books), class:"btn btn-success"%></td>
              <td><%= link_to "Destroy", book_path(@books), method: :delete, data: {confirm: "本当に消しますか？"}, class:"btn btn-danger" %></td>
            <% end %>
          </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
